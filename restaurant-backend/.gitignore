# Ignore the node_modules folder (it's huge and can be recreated with 'npm install')
node_modules/

# Ignore environment variables file (keeps your passwords and secret keys safe)
.env

# Ignore Mac system files
.DS_Store
