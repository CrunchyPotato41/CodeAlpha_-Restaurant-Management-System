// Placeholder function that logs inventory deduction (to be expanded later)
const deductInventory = (orderId) => {
  console.log(`Inventory deduction triggered for order ${orderId}`);
};

module.exports = { deductInventory };
